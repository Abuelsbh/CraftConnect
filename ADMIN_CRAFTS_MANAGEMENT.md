# صفحة إدارة أنواع الحرف - Admin Crafts Management

## نظرة عامة

صفحة بسيطة وسهلة الاستخدام تتيح للمسؤول (Admin) إدارة أنواع الحرف في التطبيق:
- ✅ عرض جميع الحرف
- ✅ إضافة حرفة جديدة
- ✅ تعديل حرفة موجودة
- ✅ حذف حرفة (تعطيلها)

## صلاحيات الأدمن (isAdmin)

**قسم إدارة الحرف متاح للأدمن فقط.** يجب أن يكون للمستخدم الحقل `isAdmin: true` في وثيقته بمجموعة `users` في Firebase.

### تعيين مستخدم كأدمن
1. افتح [Firebase Console](https://console.firebase.google.com) → مشروعك
2. اذهب إلى **Firestore Database**
3. افتح مجموعة `users`
4. اختر وثيقة المستخدم (المعرف = uid من Firebase Auth)
5. أضف حقل جديد:
   - **الاسم**: `isAdmin`
   - **النوع**: boolean
   - **القيمة**: `true`

### ملاحظات
- رابط "إدارة أنواع الحرف" يظهر في الملف الشخصي **فقط** للمستخدمين الذين لديهم `isAdmin: true`
- محاولة الوصول مباشرة لـ `/admin/crafts` بدون صلاحية أدمن تؤدي لإعادة التوجيه للصفحة الرئيسية مع رسالة خطأ

## الوصول إلى الصفحة

### الطريقة 1: من صفحة الملف الشخصي (للأدمن فقط)
1. افتح التطبيق
2. اذهب إلى صفحة **الملف الشخصي** (Profile)
3. في قائمة الإعدادات، اختر **"إدارة أنواع الحرف"** (تظهر للأدمن فقط)
4. ستفتح صفحة الإدارة

### الطريقة 2: مباشرة عبر Route
```
/admin/crafts
```
(يتطلب صلاحية أدمن)

## الميزات

### 1. عرض الحرف
- تعرض الصفحة جميع الحرف الموجودة في Firebase
- كل حرفة تعرض:
  - **الترتيب** (Order): رقم يحدد ترتيب العرض
  - **الاسم**: حسب اللغة المختارة في التطبيق
  - **القيمة** (Value): القيمة المستخدمة في الكود
  - **الترجمات**: العربية والإنجليزية
  - **حالة النشاط**: نشط أو غير نشط

### 2. إضافة حرفة جديدة
1. انقر على زر **"إضافة حرفة جديدة"** (FloatingActionButton)
2. املأ النموذج:
   - **القيمة (Value)**: القيمة المستخدمة في الكود (مثل: `plumber`)
     - يجب أن تكون فريدة
     - لا يجب أن تحتوي على مسافات
   - **الترجمة العربية**: اسم الحرفة بالعربية
   - **الترجمة الإنجليزية**: اسم الحرفة بالإنجليزية
   - **الترتيب**: رقم يحدد ترتيب العرض (الأصغر يظهر أولاً)
   - **نشط**: هل الحرفة ستظهر في التطبيق
3. انقر **"حفظ"**

### 3. تعديل حرفة موجودة
1. في قائمة الحرف، انقر على أيقونة **التعديل** (✏️) بجانب الحرفة
2. عدّل البيانات المطلوبة
   - **ملاحظة**: القيمة (Value) لا يمكن تعديلها للحرف الموجودة
3. انقر **"حفظ"**

### 4. حذف حرفة
1. في قائمة الحرف، انقر على أيقونة **الحذف** (🗑️) بجانب الحرفة
2. أكد الحذف في النافذة المنبثقة
3. **ملاحظة**: الحذف يعطل الحرفة فقط (isActive = false)، ولا يحذفها نهائياً

## مثال: إضافة حرفة جديدة

### مثال: إضافة حرفة "نجار أثاث"

1. انقر على **"إضافة حرفة جديدة"**
2. املأ النموذج:
   ```
   القيمة: furniture_carpenter
   الترجمة العربية: نجار أثاث
   الترجمة الإنجليزية: Furniture Carpenter
   الترتيب: 11
   نشط: ✓
   ```
3. انقر **"حفظ"**

**النتيجة:**
- ستظهر الحرفة الجديدة في قائمة الحرف
- ستظهر في صفحة تسجيل العطل
- ستظهر في صفحة تعديل الملف الشخصي للحرفي
- الترجمة ستتغير تلقائياً حسب لغة التطبيق

## مثال: تعديل حرفة موجودة

### مثال: تحديث ترجمة حرفة "نجار"

1. ابحث عن حرفة "نجار" في القائمة
2. انقر على أيقونة **التعديل** (✏️)
3. عدّل الترجمة العربية من "عطل نجارة" إلى "نجار محترف"
4. انقر **"حفظ"**

**النتيجة:**
- ستتحدث الترجمة في جميع أنحاء التطبيق فوراً

## مثال: تعطيل حرفة

### مثال: تعطيل حرفة "ستالايت"

1. ابحث عن حرفة "ستالايت" في القائمة
2. انقر على أيقونة **الحذف** (🗑️)
3. أكد الحذف

**النتيجة:**
- لن تظهر الحرفة في التطبيق
- لكن البيانات ستبقى محفوظة في Firebase
- يمكن إعادة تفعيلها لاحقاً

## تحديث القائمة

- انقر على أيقونة **"تحديث"** (🔄) في شريط التطبيق العلوي
- أو اسحب القائمة للأسفل (Pull to Refresh)

## واجهة المستخدم

### الألوان والدلالات
- **أزرق** (✏️): تعديل
- **أحمر** (🗑️): حذف
- **أخضر**: نجاح العملية
- **أحمر**: خطأ في العملية
- **رمادي**: حرفة غير نشطة

### الحالات
- **تحميل**: دائرة تحميل في المنتصف
- **خطأ**: رسالة خطأ مع زر "إعادة المحاولة"
- **فارغ**: رسالة "لا توجد حرف"
- **قائمة**: عرض جميع الحرف

## ملاحظات مهمة

1. **القيمة (Value) فريدة**: لا يمكن استخدام نفس القيمة لحرفتين مختلفتين
2. **الترجمات مطلوبة**: يجب إدخال الترجمة العربية والإنجليزية
3. **الترتيب مهم**: استخدم أرقام متسلسلة لتحديد ترتيب العرض
4. **التحديثات الفورية**: التغييرات تظهر فوراً في التطبيق
5. **الحذف = التعطيل**: الحذف يعطل الحرفة فقط، ولا يحذفها نهائياً

## الأمان

⚠️ **ملاحظة**: حالياً، الصفحة متاحة لجميع المستخدمين. يمكنك:

1. **إضافة فحص Admin**: تحقق من أن المستخدم مسؤول قبل السماح بالوصول
2. **إخفاء الرابط**: إخفاء رابط "إدارة أنواع الحرف" من صفحة Profile للمستخدمين العاديين
3. **قواعد Firestore**: تأكد من أن قواعد Firestore تمنع المستخدمين العاديين من الكتابة

### مثال: إضافة فحص Admin

```dart
// في profile_screen.dart
MenuItem(
  icon: Icons.admin_panel_settings,
  title: 'إدارة أنواع الحرف',
  onTap: () {
    final authProvider = Provider.of<SimpleAuthProvider>(context, listen: false);
    final currentUser = authProvider.currentUser;
    
    // فحص إذا كان المستخدم admin
    if (currentUser?.email == 'admin@example.com' || 
        currentUser?.userType == 'admin') {
      context.push('/admin/crafts');
    } else {
      ScaffoldMessenger.of(context).showSnackBar(
        const SnackBar(content: Text('ليس لديك صلاحية للوصول')),
      );
    }
  },
),
```

## استكشاف الأخطاء

### المشكلة: لا تظهر الحرف
**الحل:**
- تحقق من اتصال الإنترنت
- انقر على زر "تحديث"
- تحقق من قواعد Firestore

### المشكلة: خطأ في الإضافة/التعديل
**الحل:**
- تحقق من أن جميع الحقول مملوءة
- تحقق من أن القيمة (Value) فريدة
- تحقق من قواعد Firestore (يجب السماح بالكتابة)

### المشكلة: التغييرات لا تظهر في التطبيق
**الحل:**
- أعد تشغيل التطبيق
- تحقق من أن الحرفة `isActive = true`
- تحقق من اتصال Firebase

## الدعم

للمزيد من المعلومات، راجع:
- `lib/Modules/Admin/admin_crafts_management_screen.dart` - كود الصفحة
- `lib/services/craft_service.dart` - خدمة الحرف
- `lib/Models/craft_model.dart` - نموذج الحرفة
- `FIREBASE_CRAFTS_SETUP.md` - دليل إعداد Firebase







